# NGSPICE Faithful Models — Gap Analysis

**Merged into `docs/GAPS.md` (2026-07-01).** This file's content (Parts
N, O, P, Q) has been folded into the main gap-tracking document — most of
it (all of Part P, `$temperature`/`$vt`/`I(a,b)` reads, noise stamping)
is now done and dropped; the remainder lives under `docs/GAPS.md`'s
Parts A, D, E, H, and the new Part M (model-specific numeric fidelity).

See `docs/GAPS.md` for the current, accurate list of open NGSPICE
fidelity gaps.
