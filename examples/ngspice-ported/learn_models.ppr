`include "ngspice.ppr"

// Ported from ngspice by tools/spice2ppr.py — see examples/ngspice-ported/README.md
// Source netlist: models.cir

module gain(in, out, k_1);
    vcvs #(.gain({K})) E1(.p(out), .n(gnd), .cp(in), .cn(gnd));
endmodule

module sum(in1, in2, out, k1_1, k2_1, a_1);
    // behavioral source: v = {a * ((v(in1) * k1) + (v(in2) * k2))}
      // bsource_v #(.v("...")) b1(...);
endmodule

module models;

    initial begin
        $op();
    end
endmodule
