discipline Bit {
    storage Boolean;
}

mod MatchTest(input clk : Bit) {
    var m : Boolean = 0;
    var x : Bit = 0;
}

digital MatchTest {
    @posedge(clk) {
        match m {
            _ => {
                x = 1;
            }
        }
    }
}
