discipline Bit {
    storage Boolean;
}

mod AssertTest(input a : Bit) { }

digital AssertTest {
    if (a == 1) {
        $info ("a is 1");
    }
}

bench AssertTest {
    fn test_op_solves() {
        var res = $op();
        $assert(1 == 1, "AssertTest reaches a DC operating point");
    }
}
