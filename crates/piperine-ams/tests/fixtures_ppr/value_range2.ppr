// This program is about testing that the value ranges parse and work for
// integer parameters.

mod test ( input in : Bit );

    param foo : Real = 0.0; // from[-10.0:10.0] exclude[1:2]
    param bar : Real = 0.0; // from(-inf:0]

    initial begin
        $display("foo = %f", foo);
        $display("PASSED");
        $finish;
    end
}

mod main;

digital main {
    var rrr : Bit = 0;
    // test #(.foo(2),.bar(-5.0)) dut(rrr);
}

endmodule