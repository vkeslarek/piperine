// This program is about testing that the value ranges parse and work for
// integer parameters.

mod test ( input in : Bit );

    param foo : Integer = 0; // from[-10:10] exclude[1:2]
    param bar : Integer = 0; // from(-inf:0]

    initial begin
        $display("foo = %d", foo);
        $display("PASSED");
        $finish;
    end
}

mod main;

digital main {
    var rrr : Bit = 0;
    // test #(.foo(2),.bar(-5)) dut(rrr);
}

endmodule