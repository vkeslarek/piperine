[package]
name = "piperine-cli"
version.workspace = true
edition.workspace = true

[dependencies]
clap = { version = "4.6.1", features = ["derive"] }
piperine-ams = { version = "0.1.0", path = "../piperine-ams" }
piperine-project = { version = "0.1.0", path = "../piperine-project" }
